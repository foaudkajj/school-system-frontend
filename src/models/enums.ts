export enum Attendance {
  Available = "Available",
  Late = "Late",
  Absent = "Absent",
}

export enum Evaluation {
  Good = "Good",
  Mid = "Mid",
  Bad = "Bad",
}

export enum Gender {
  Male = "Male",
  Female = "Female",
}
